---
// English home page (default locale, served at the root). All markup lives in
// the shared Home component; localized copies come from [locale]/index.astro.
import Home from '../components/Home.astro';
---

<Home />
