# Converter quality report

> Generated by an LLM judge over the adversarial eval corpus (`scripts/judge-fixtures.mjs`). Each fixture pairs an intended document spec with the converter’s actual Markdown; the judge scores fidelity and lists concrete issues. This is a prioritized backlog of real converter deficiencies, not a pass/fail gate.

## Summary

- Fixtures judged: **24**
- Mean fidelity score: **0.80** / 1.00
- Issues: **15 high**, **20 medium**, **6 low**

## Top issues (by severity)

- **[high]** (city-budget-brief-multiple-footnotes-body-paragraphs-v2) Each footnote marker in the body incorrectly includes the literal source numeral before the superscript link (e.g. `enforcement1<sup>...`), duplicating the footnote number instead of rendering a single inline footnote reference.
- **[high]** (deployment-notes-inline-code-and-plain-text-url) The URL-looking text was converted into an autolink (`<https://status.example.net/rollback>`), but it should remain plain text while italicized, not a hyperlink.
- **[high]** (deployment-notes-inline-code-and-plain-url-text) The plain-text URL-looking string was converted into an autolink (`<https://status.internal.example/local>`), but it should remain plain text and not become a hyperlink.
- **[high]** (editor-note-smart-quotes-em-dash-nbsp-variant-a) Smart quotes/apostrophes were replaced with straight ASCII quotes in both paragraphs, changing intended punctuation and typography.
- **[high]** (editor-note-smart-quotes-em-dash-nbsp-variant-a) Em dashes were incorrectly converted to hyphens in both quoted phrases and before `a small change`, altering the intended punctuation.
- **[high]** (incident-postmortem-nested-remediation-list-v1) The nested list hierarchy lost the intended alternation between ordered and unordered list levels. Levels 1 and 3 should be unordered bullets, but the output renders every level as ordered lists.
- **[high]** (maintenance-window-table-vertical-merge) The vertically merged 'Production' cell was not preserved correctly. The second maintenance row is malformed and missing the empty Environment placeholder, causing the table to have only two cells in that row.
- **[high]** (maintenance-window-table-vertical-merge) The second data row is shifted left: 'Thu 21:30-22:15 UTC' appears under Environment and 'Load balancer failover test' under Window, with the Scope cell lost entirely.
- **[high]** (museum-label-smart-punctuation-nbsp) Em dashes were converted to hyphens in all three paragraphs, changing punctuation and typography (e.g., `back”—a` became `back"-a`).
- **[high]** (quarterly-sales-table-colspan-header) The table separator row is malformed: `| --- | --- | | --- | | --- | --- |` has the wrong number of cells and empty separator entries, producing an invalid/broken Markdown table.
- **[high]** (regional-sales-table-merged-quarter-header) The table separator/alignment row is malformed: it contains more cells than the header row and includes empty alignment cells (`| | |`), which makes the Markdown table invalid or inconsistently rendered.
- **[high]** (regional-sales-table-merged-quarter-header) The second header row is missing the leading empty placeholder cell needed to represent the row-spanned 'Region' header, causing the month labels to shift left under the wrong columns.
- **[high]** (release-checklist-table-with-vertical-owner-merge) The vertically merged Owner cell was not preserved correctly. The second task row should repeat or otherwise retain 'Mina Patel' as the owner, but the output instead loses that association.
- **[high]** (release-checklist-table-with-vertical-owner-merge) The second table row is structurally broken: 'Verify production feature flags' was placed in the Owner column and 'In progress' in the Task column, leaving the Status column missing.
- **[high]** (vendor-shortlist-links-in-lists-and-table-cells) In the Aster Networks table row, the first link in the Reference Links cell was mangled as `[](https://www.aster-networks.example/catalog)` with no visible linked text, so the cell content is not faithfully represented.
- **[medium]** (city-budget-brief-multiple-footnotes-body-paragraphs-v2) The inline footnote markup is malformed/noisy: `1<sup>[\[1\]](#footnote-1)</sup>` shows escaped brackets inside `<sup>` rather than clean Markdown like `<sup>[1](#footnote-1)</sup>` or standard GFM footnote syntax.
- **[medium]** (city-budget-footnotes-in-body-paragraphs-v1) Footnotes are rendered as raw HTML superscript links plus a numbered list instead of clean GitHub-Flavored Markdown footnote syntax (`[^1]` / `[^1]: ...`).
- **[medium]** (deployment-notes-inline-code-and-plain-text-url) All underlined spans were flattened to plain text. Underline formatting is missing for `NODE_ENV=production`, `npm run migrate`, `systemctl status api`, `active (running)`, and `/var/log/api/error.log`.
- **[medium]** (deployment-notes-inline-code-and-plain-url-text) All underlined inline runs were lost: `NODE_ENV=production`, `npm run migrate`, `APP_PORT=8080`, `.env.production`, `systemctl restart api.service`, and `git revert --no-edit HEAD` should preserve underline styling.
- **[medium]** (editor-note-smart-quotes-em-dash-nbsp-variant-a) The heading apostrophe was normalized from curly to straight: `Editor’s Note` became `Editor's Note`.
- **[medium]** (editor-note-smart-quotes-em-dash-nbsp-variant-a) Non-breaking spaces in `May 1` and `after 9 p.m.` were collapsed to regular spaces, losing the intended no-wrap behavior.
- **[medium]** (incident-postmortem-nested-remediation-list-v1) Because all nested levels are numbered, list semantics are changed in multiple places, including under 'Stabilize production', 'Investigate root cause', and 'Document preventive actions'. This is a structural fidelity issue, not just a formatting preference.
- **[medium]** (lab-notes-inline-formatting-observation) Underline formatting on "sealed vial" was lost. In GFM this could be preserved with inline HTML, e.g. `<u>sealed vial</u>`.
- **[medium]** (maintenance-window-table-vertical-merge) En dashes in time ranges were converted to hyphens ('22:00-23:00', '21:30-22:15', '18:00-18:30') instead of preserving the original punctuation.
- **[medium]** (museum-exhibit-installation-checklist-nested-mixed-list) The nested list lost the intended mixed ordered/unordered structure. All sublevels were rendered as ordered lists, but the source alternates between ordered and unordered items across levels.
- **[medium]** (museum-label-smart-punctuation-nbsp) Smart quotes were normalized to straight ASCII quotes in all three paragraphs; the intended curly quotation marks should be preserved.
- **[medium]** (museum-label-smart-punctuation-nbsp) Non-breaking spaces were lost in `least 2`, `page 14`, and `c. 1880`, which may allow unwanted line breaks and does not preserve the source text faithfully.
- **[medium]** (quarterly-sales-table-colspan-header) The merged header cells are represented only by empty placeholder columns (`| North America | | Europe | |`), which does not cleanly convey the intended horizontal grouping in GitHub-Flavored Markdown.
- **[medium]** (regional-sales-table-merged-quarter-header) Currency values were unnecessarily escaped as `\$118k` etc. The intended content is plain `$118k`; while often rendered similarly, this is not faithful Markdown output.
- **[medium]** (regional-sales-table-merged-quarter-header) Merged header structure is not cleanly preserved in valid GFM. Since Markdown cannot truly merge cells, the converter should approximate it with a consistent 7-column table using blank placeholder cells rather than producing broken syntax.
- **[medium]** (release-checklist-table-cell-bullets-v1) The bullet list inside the Payments/Open items table cell was flattened into a single line, losing the intended multi-item structure. In GFM, preserve line breaks within the cell (for example with `<br>` separators) so each bullet remains distinct.
- **[medium]** (release-checklist-table-cell-nested-bullets) The three bullet-style lines in the 'Open risks' cell were flattened into a single line. They should remain distinct in-cell lines, e.g. separated with `<br>` in Markdown to preserve the original cell structure.
- **[medium]** (release-checklist-table-first-row-header-2) Underline formatting on the word "latest" in the final table row was lost. In clean GFM this should be preserved with inline HTML such as `<u>latest</u>`.
- **[medium]** (release-checklist-table-with-vertical-owner-merge) The Markdown table has an inconsistent number of columns in the second data row, which makes the table malformed in GitHub-Flavored Markdown.
- **[medium]** (vendor-shortlist-links-in-lists-and-table-cells) The Aster Networks Reference Links cell lost spacing around the first link, producing `Catalog [](…)and ...` instead of preserving readable separation between `Catalog`, the linked content, and `and`.
- **[low]** (city-budget-brief-multiple-footnotes-body-paragraphs-v2) The footnote list adds backlink arrows and anchor targets not present in the intended content. While not content loss, this introduces extra output beyond the source document.
- **[low]** (city-budget-footnotes-in-body-paragraphs-v1) The dollar sign in `$3.5 million` is unnecessarily escaped as `\$3.5 million`, reducing Markdown cleanliness.
- **[low]** (city-budget-footnotes-in-body-paragraphs-v1) Back-reference anchors (`#footnote-ref-*`) are included, but no matching reference IDs are defined in the body, so the return links are not valid.
- **[low]** (lab-notes-inline-formatting-observation) Bold+italic formatting for "38.6%" is represented as `**_38.6%_**` instead of the cleaner equivalent `***38.6%***`. Fidelity is preserved, but the Markdown is less clean than ideal.
- **[low]** (quarterly-sales-table-colspan-header) Dollar signs in body cells are unnecessarily escaped (`\$1.2M`, `\$1.4M`), reducing fidelity to the source text.
- **[low]** (release-checklist-links-in-lists-and-table-cells-v1) Table formatting uses padded column widths, which is acceptable GFM but not the cleanest minimal Markdown representation.

## Per-fixture scores (lowest first)

| Fixture                                                   | Score | Challenge                                                                                                                                                                  |
| --------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `editor-note-smart-quotes-em-dash-nbsp-variant-a`         | 0.42  | A short editorial note uses curly quotes, em dashes, and non-breaking spaces within paragraphs, which can be altered or normalized incorrectly during conversion.          |
| `regional-sales-table-merged-quarter-header`              | 0.42  | A compact report uses a table whose top header row merges quarter labels across multiple columns before a single body row of regional sales data.                          |
| `maintenance-window-table-vertical-merge`                 | 0.56  | A compact operations note includes a table where the environment cell is vertically merged across two maintenance rows.                                                    |
| `quarterly-sales-table-colspan-header`                    | 0.56  | A compact report table uses horizontally merged header cells to group quarterly columns above a single body row.                                                           |
| `release-checklist-table-with-vertical-owner-merge`       | 0.58  | A release checklist uses a table where the owner cell is vertically merged across two rows, which can be misread when converting to Markdown.                              |
| `deployment-notes-inline-code-and-plain-text-url`         | 0.62  | This document is tricky because it mixes inline code-like spans in prose with a URL-looking string that is intentionally plain text rather than a hyperlink.               |
| `deployment-notes-inline-code-and-plain-url-text`         | 0.62  | A short deployment note mixes several inline code spans with a plain-text URL-looking string that should not become a link.                                                |
| `museum-label-smart-punctuation-nbsp`                     | 0.62  | A short museum-style document where paragraphs mix smart quotes, em dashes, and non-breaking spaces that must survive conversion without normalization.                    |
| `city-budget-brief-multiple-footnotes-body-paragraphs-v2` | 0.72  | A short policy brief with several body paragraphs that each contain multiple footnote references, testing how repeated inline notes are preserved across paragraphs.       |
| `incident-postmortem-nested-remediation-list-v1`          | 0.72  | A postmortem document with a deeply nested list that alternates between ordered and unordered items across four levels.                                                    |
| `vendor-shortlist-links-in-lists-and-table-cells`         | 0.86  | A procurement note mixes hyperlinks within bulleted list items and inside table cells, including multiple links in a single cell and linked text alongside plain text.     |
| `city-budget-footnotes-in-body-paragraphs-v1`             | 0.90  | Two body paragraphs each contain multiple footnote references inline, requiring the converter to preserve several distinct notes attached to ordinary prose.               |
| `release-checklist-table-cell-bullets-v1`                 | 0.90  | A compact status table includes a cell whose content is a nested bullet list, which is difficult to flatten cleanly into Markdown while preserving table structure.        |
| `release-checklist-table-cell-nested-bullets`             | 0.90  | A compact release-planning document where one table cell contains multiple bullet-style lines that should remain associated with that single cell during conversion.       |
| `lab-notes-inline-formatting-observation`                 | 0.94  | A short lab note uses a single paragraph that mixes bold, italic, strikethrough, superscript, and subscript inline around technical notation.                              |
| `release-checklist-table-first-row-header-2`              | 0.94  | A compact project-status document where the table’s first row is the actual header row and subsequent rows mix plain text with linked and formatted content.               |
| `museum-exhibit-installation-checklist-nested-mixed-list` | 0.96  | A compact exhibit-planning document centered on a deeply nested list that alternates between ordered and unordered items across four levels.                               |
| `release-checklist-links-in-lists-and-table-cells-v1`     | 0.97  | A short project document where hyperlinks appear within bulleted checklist items and inside table cells alongside plain text.                                              |
| `field-report-heading-jumps-after-table`                  | 1.00  | A short field report uses non-sequential heading levels and places a lower-level heading immediately after a table, which can confuse Markdown heading structure.          |
| `lab-note-inline-mixed-formatting-observation`            | 1.00  | A short lab note uses a single paragraph that mixes bold, italic, strikethrough, superscript, and subscript inline around technical content.                               |
| `ordered-list-restart-after-note-project-checklist`       | 1.00  | An ordered list is interrupted by a normal paragraph and then resumes as a restarted numbered list, which can be mistaken for continuous numbering in Markdown conversion. |
| `quarterly-ops-headings-skip-after-table-v1`              | 1.00  | A short operations memo where headings skip from H1 to H3 and another heading appears immediately after a table.                                                           |
| `release-checklist-ordered-list-restarts-after-note`      | 1.00  | An ordered list is interrupted by a normal paragraph note, then resumes with numbering restarted at 1, which can be misread as a continuation in Markdown conversion.      |
| `release-checklist-table-first-row-header`                | 1.00  | A compact status table uses the first row as labeled column headers, which should become the Markdown header row during conversion.                                        |
